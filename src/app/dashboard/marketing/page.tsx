export default function MarketingPage() {
  return (
    <div className="rounded-lg border border-[var(--border)] bg-[var(--surface-1)] p-8 text-center">
      <h2 className="text-sm font-medium text-[var(--text-primary)]">Marketing</h2>
      <p className="mt-1 text-sm text-[var(--text-muted)]">
        Ainda não conectado — combinar com o Rodrigo quais métricas de marketing entram aqui (ex: origem
        de tráfego, campanhas, CAC) antes de desenhar essa aba.
      </p>
    </div>
  );
}
